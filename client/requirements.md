## Packages
framer-motion | Page transitions and scroll-triggered animations
embla-carousel-react | For the hero image carousel and mobile testimonials
embla-carousel-autoplay | For auto-sliding hero carousel

## Notes
Static images from the siteContent JSON are assumed to be in the `client/public` folder.
Form submission posts directly to `/api/send-email` using TanStack Query.
